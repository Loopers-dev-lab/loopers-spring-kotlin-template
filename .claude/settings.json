{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit:*)",
        "hooks": [
          {
            "type": "command",
            "command": "./gradlew ktlintCheck -q 2>&1; KTLINT=$?; ./gradlew :apps:commerce-api:test -q 2>&1; TEST=$?; if [ $KTLINT -ne 0 ] || [ $TEST -ne 0 ]; then echo 'VALIDATION_FAILED: ktlint='$KTLINT' test='$TEST; exit 1; fi"
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": [
      "Bash(git push -f:*)",
      "Bash(git reset --hard:*)"
    ]
  }
}

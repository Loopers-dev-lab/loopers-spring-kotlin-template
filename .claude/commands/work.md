---
description: Execute next milestone from plan.md using worker agent
---

Run worker agent to execute the next unchecked milestone.

If `$ARGUMENTS` is "auto", continue until failure or completion.